Spidey Template is basically a starter template code for node.js + mongodb.

This codebase stucture is highly inspired by GO codebase structure.

How can you use it?

1. Fork and clone the repository
2. ```
     npm install
   ```
3. ```
     npm run dev
   ```
